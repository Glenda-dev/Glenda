// Boot entrypoints currently come from the shared HAL API stub and the
// bootloader-specific modules under `kernel/src/boot/`. This module stays empty
// until the x86_64-specific early entry sequence is implemented.
