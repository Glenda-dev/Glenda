[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
bitflags = "2.10.0"
fdt = "0.1.5"
num_enum = { version = "0.7.5", default-features = false }

[build-dependencies]
chrono = "0.4"

[features]
default = ["logging"]
multiboot2 = []
logging = []
shell = []
gdb = []
