[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
bitflags = "2.10.0"
fdt = "0.1.5"

[build-dependencies]
chrono = "0.4"

[features]
default = ["logging", "shell"]
multiboot2 = []
logging = []
shell = []
